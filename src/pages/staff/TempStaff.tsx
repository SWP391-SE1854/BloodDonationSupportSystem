import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

const TempStaff = () => {
  return (
    <div className="p-8">
      <Card>
        <CardHeader>
          <CardTitle>Temporary Staff Page</CardTitle>
        </CardHeader>
        <CardContent>
          <p>This is a temporary page for Staff functionalities. The main portal is currently under maintenance.</p>
        </CardContent>
      </Card>
    </div>
  );
};

export default TempStaff; 